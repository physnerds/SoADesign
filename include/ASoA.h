#ifndef ASOA_H
#define ASOA_H 1

#include "soa.h"
#include <vector>

namespace soa{
class ASoA{

    public:
    ASoA(){};
    ~ASoA(){};
    static const int soa_size = 500;
    static const int subsoa_size = 500;
    Generate_Arrays(msoa,
                    Addvector(uint32_t, a);  
                    Addvector(uint32_t, b);
                      
                    );

    Generate_Arrays(asoa,
                    Addvector(uint32_t, aa);
                    AddSoA(msoa, subsoa);

                );

    private:
    


};

}
#endif